Tags: #Tag_基础理论 
# Information
---
- DeepSeek

# Mainly Idea
---
### 1️⃣ 现有大模型的一个“结构性浪费”

当前主流大模型（尤其是 **MoE：Mixture of Experts**）有一个隐含问题：

> **模型在用“昂贵的计算”，反复去“重建本来是静态的知识”。**

举个例子：
当模型看到 **“Diana, Princess of Wales”** 这种固定实体时，它并不是“查表得到”，而是要在**多层 attention + FFN 中逐步拼出来**。
这意味着：

* 前几层本该用于**推理**
* 却被迫做了**记忆恢复**这种低级工作

👉 **深度被浪费了**

---

### 2️⃣ MoE 只能解决一半问题

MoE 的优势是：

* 稀疏计算
* 用很少的 FLOPs 激活很大的参数空间

但 MoE 只擅长一件事：

> **“如何算得更聪明”**

而不擅长：

> **“如何记得更便宜”**

---

## 二、Engram 的核心思想（一句话版）

> **给 Transformer 加一个“真正的、O(1) 的、可扩展的记忆查表机制”，
> 让模型：**
>
> * 静态知识 👉 用查表
> * 动态推理 👉 用计算

论文把它称为：
👉 **Conditional Memory（条件记忆）**

---

## 三、Engram 是怎么做的？（结构直觉版）

### 🔹 1. 用 N-gram 作为“记忆地址”

* 不看全句
* 只看 **最近的 2~3 个 token（suffix N-gram）**
* 例如：

  ```
  Alexander the Great
  Princess of Wales
  四大发明
  ```

这些**高度模板化、局部、稳定**的模式，特别适合查表。

---

### 🔹 2. 哈希 + 超大 embedding 表 = O(1) 记忆访问

* 不枚举所有 N-gram（不现实）
* 用 **多头哈希（multi-head hashing）**
* 每个 token **只查固定几个槽位**

结果是：

| 维度         | 成本           |
| ---------- | ------------ |
| 记忆规模       | 可做到 **百亿参数** |
| 每 token 计算 | **常数 O(1)**  |
| FLOPs      | 几乎不增长        |

---

### 🔹 3. 用“门控”避免乱查、误查

静态记忆的问题是：
**它不懂上下文**

解决方法：

* 当前 hidden state = **Query**
* 查到的记忆 = **Key / Value**
* 做一个 **点积门控（sigmoid）**

效果是：

> ✔ 语义匹配 → 打开门
> ✖ 语义不符 → 直接关掉

**这一步非常关键**，否则哈希噪声会毁掉模型。

# Question
---


# Reference
---


# Attachment
---
![[Engram_paper.pdf]]